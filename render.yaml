services:
  - type: web
    name: dogal-urunler
    runtime: node
    buildCommand: npm install && node server/seed.js
    startCommand: node server/app.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000
